export enum EventType {
  Click,
  DblClick,
  WebSocket,
  Mqtt,
}

export enum EventAction {
  Link,
  StartAnimate,
  Function,
  WindowFn,
  SetProps,
  PauseAnimate,
  StopAnimate,
}
